/* 攻略页共享样式 (ticket-guide / traffic-guide / blessing-guide) */

page {
  background: #F2F4F7;
  margin: 0;
  padding: 0;
}

.page {
  min-height: 100vh;
  padding-bottom: 176rpx;
  box-sizing: border-box;
}

/* ===== 导航栏 ===== */
.nav-bar {
  background: linear-gradient(90deg, #6B5A7A 0%, #8B6A5C 100%);
}

.nav-bar-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 24rpx;
}

.nav-back,
.nav-placeholder {
  width: 88rpx;
  height: 100%;
  display: flex;
  align-items: center;
}

.nav-back { justify-content: flex-start; }
.nav-placeholder { justify-content: flex-end; }

.nav-back-icon {
  font-size: 56rpx;
  line-height: 1;
  color: #FFFFFF;
}

.nav-title {
  flex: 1;
  text-align: center;
  font-size: 32rpx;
  font-weight: 600;
  color: #FFFFFF;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* ===== 页面 Banner ===== */
.page-banner {
  background: linear-gradient(135deg, #6B5A7A 0%, #8B6A5C 60%, #C08A60 100%);
  padding: 40rpx 32rpx 48rpx;
}

.banner-title {
  font-size: 40rpx;
  font-weight: 800;
  color: #FFFFFF;
  line-height: 1.3;
}

.banner-sub {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.85);
  margin-top: 8rpx;
}

/* ===== 通用区块 ===== */
.section {
  margin: 18rpx 24rpx 0;
  background: #FFFFFF;
  border-radius: 16rpx;
  padding: 28rpx 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.section-head {
  margin-bottom: 20rpx;
}

.section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #1A1A1A;
}

/* ===== FAQ 列表 ===== */
.faq-list {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.faq-item {
  border-bottom: 1rpx solid #F0F1F3;
  padding: 20rpx 0;
}

.faq-item:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

.faq-item:first-child {
  padding-top: 0;
}

.faq-q {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.faq-q-text {
  flex: 1;
  font-size: 28rpx;
  font-weight: 600;
  color: #1A1A1A;
  line-height: 1.4;
}

.faq-arrow {
  font-size: 36rpx;
  color: #C0C4CC;
  flex-shrink: 0;
  width: 40rpx;
  text-align: center;
}

.faq-a {
  margin-top: 12rpx;
  padding: 16rpx 20rpx;
  background: #F7F8FA;
  border-radius: 12rpx;
}

.faq-a text {
  font-size: 26rpx;
  color: #4A4A4A;
  line-height: 1.7;
}

/* ===== 建议列表 ===== */
.tip-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.tip-item {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
}

.tip-dot {
  width: 10rpx;
  height: 10rpx;
  margin-top: 14rpx;
  border-radius: 50%;
  background: #2577E3;
  flex-shrink: 0;
}

.tip-text {
  flex: 1;
  font-size: 26rpx;
  line-height: 1.75;
  color: #4A4A4A;
}

/* ===== 导游引导区 ===== */
.guide-cta-section {
  margin: 18rpx 24rpx 0;
  background: linear-gradient(135deg, #F5F0EB 0%, #EDE7E0 100%);
  border-radius: 16rpx;
  padding: 32rpx 24rpx;
  text-align: center;
}

.guide-cta-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #1A1A1A;
}

.guide-cta-desc {
  font-size: 24rpx;
  color: #666;
  margin-top: 8rpx;
}

/* ===== 页面底部描述 ===== */
.page-footer-desc {
  padding: 12rpx 24rpx;
  font-size: 16rpx;
  color: #DCDFE3;
  line-height: 1.5;
}
