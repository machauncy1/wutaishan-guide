<!--pages/ticketGuide/index.wxml-->
<view class="page">

  <!-- Banner（导航栏嵌入 banner 内部，图片通顶） -->
  <view class="page-banner">
    <image
      class="page-banner-img {{bannerLoaded ? 'page-banner-img-loaded' : ''}}"
      src="{{bannerImage}}"
      mode="aspectFill"
      bindload="onBannerLoad"
      wx:if="{{bannerImage}}"
    />
    <view class="page-banner-gradient"></view>
    <view class="page-banner-content">
      <view style="height: {{statusBarHeight}}px"></view>
      <view class="page-banner-nav" style="height: {{navBarHeight}}px">
        <view class="nav-back" bindtap="onBack">
          <text class="nav-back-icon">‹</text>
        </view>
        <view class="nav-title">门票攻略</view>
        <view class="nav-placeholder"></view>
      </view>
      <view class="banner-title">五台山门票怎么买</view>
      <view class="banner-sub">游客服务中心购票及在线购票全指南</view>
    </view>
  </view>

  <!-- 常见问题 -->
  <view class="section">
    <view class="section-head">
      <text class="section-title">常见问题</text>
    </view>

    <view class="faq-list">
      <view class="faq-item {{activeIndex === 0 ? 'faq-active' : ''}}" bindtap="onFaqTap" data-index="0">
        <view class="faq-q">
          <text class="faq-q-text">五台山门票多少钱？</text>
          <text class="faq-arrow">{{activeIndex === 0 ? '−' : '+'}}</text>
        </view>
        <view class="faq-a" wx:if="{{activeIndex === 0}}">
          <text>进山门票135元/人，不分淡旺季，全年统一价格。</text>
        </view>
      </view>

      <view class="faq-item {{activeIndex === 1 ? 'faq-active' : ''}}" bindtap="onFaqTap" data-index="1">
        <view class="faq-q">
          <text class="faq-q-text">需要提前几天买票？</text>
          <text class="faq-arrow">{{activeIndex === 1 ? '−' : '+'}}</text>
        </view>
        <view class="faq-a" wx:if="{{activeIndex === 1}}">
          <text>建议至少提前1天在线购票，五一、十一等高峰期建议提前2-3天预订。</text>
        </view>
      </view>

      <view class="faq-item {{activeIndex === 2 ? 'faq-active' : ''}}" bindtap="onFaqTap" data-index="2">
        <view class="faq-q">
          <text class="faq-q-text">当天能直接买票进吗？</text>
          <text class="faq-arrow">{{activeIndex === 2 ? '−' : '+'}}</text>
        </view>
        <view class="faq-a" wx:if="{{activeIndex === 2}}">
          <text>淡季一般可以，旺季可能限流。建议提前在线购票，也可到五台山游客服务中心现场购买，但排队时间较长。</text>
        </view>
      </view>

      <view class="faq-item {{activeIndex === 3 ? 'faq-active' : ''}}" bindtap="onFaqTap" data-index="3">
        <view class="faq-q">
          <text class="faq-q-text">景区会限流吗？</text>
          <text class="faq-arrow">{{activeIndex === 3 ? '−' : '+'}}</text>
        </view>
        <view class="faq-a" wx:if="{{activeIndex === 3}}">
          <text>节假日高峰期会限流，日常一般不限流。具体以景区当日公告为准。</text>
        </view>
      </view>

      <view class="faq-item {{activeIndex === 4 ? 'faq-active' : ''}}" bindtap="onFaqTap" data-index="4">
        <view class="faq-q">
          <text class="faq-q-text">门票包含哪些景点？</text>
          <text class="faq-arrow">{{activeIndex === 4 ? '−' : '+'}}</text>
        </view>
        <view class="faq-a" wx:if="{{activeIndex === 4}}">
          <text>进山门票包含景区内所有寺庙参观，无需额外购票。</text>
        </view>
      </view>

      <view class="faq-item {{activeIndex === 5 ? 'faq-active' : ''}}" bindtap="onFaqTap" data-index="5">
        <view class="faq-q">
          <text class="faq-q-text">我该买哪天的门票？</text>
          <text class="faq-arrow">{{activeIndex === 5 ? '−' : '+'}}</text>
        </view>
        <view class="faq-a" wx:if="{{activeIndex === 5}}">
          <text>建议根据行程确定入山日期后再购票。导游可以帮您规划日期和行程，避免买错日期。</text>
        </view>
      </view>

      <view class="faq-item {{activeIndex === 6 ? 'faq-active' : ''}}" bindtap="onFaqTap" data-index="6">
        <view class="faq-q">
          <text class="faq-q-text">景区附近住宿方便吗？</text>
          <text class="faq-arrow">{{activeIndex === 6 ? '−' : '+'}}</text>
        </view>
        <view class="faq-a" wx:if="{{activeIndex === 6}}">
          <text>台怀镇有各档位住宿，旺季建议提前预订。导游可根据预算推荐合适的住宿。</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 购票建议 -->
  <view class="section">
    <view class="section-head">
      <text class="section-title">购票建议</text>
    </view>
    <view class="tip-list">
      <view class="tip-item">
        <text class="tip-dot"></text>
        <text class="tip-text">线上购票更方便，到五台山游客服务中心扫码入园</text>
      </view>
      <view class="tip-item">
        <text class="tip-dot"></text>
        <text class="tip-text">随身携带身份证，购票和入园均需要</text>
      </view>
      <view class="tip-item">
        <text class="tip-dot"></text>
        <text class="tip-text">节假日建议提前购票，避免限流无法入园</text>
      </view>
    </view>
  </view>

  <!-- 导游安排引导 -->
  <view class="guide-cta-section">
    <view class="guide-cta-title">门票 + 行程，导游统一安排</view>
    <view class="guide-cta-desc">购票、路线、讲解一站式安排，省心出行</view>
  </view>

</view>

<bottom-contact-bar
  phone="{{contactPhone}}"
  show-consult="{{true}}"
  consult-label="咨询导游"
  phone-label="电话沟通"
/>
